# ``SymbolSource``

The contract a symbol source implements to feed the index.

## Overview

A source enumerates and extracts its own frameworks. The indexing service drives
one or more sources into the store, so adding a new kind of source needs no
change to the store or the app.

## Topics

### The source contract

- ``SymbolSource``
